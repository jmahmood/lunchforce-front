import { SalesforceOnlyDirective } from './salesforce-only.directive';

describe('SalesforceOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new SalesforceOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
